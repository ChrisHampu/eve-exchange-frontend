export function updateMarketGroups(market_groups) {
  return { type: "UPDATE_MARKET_GROUPS", market_groups };
}

export function updateBlueprints(blueprints) {
  return { type: "UPDATE_BLUEPRINTS", blueprints };
}
