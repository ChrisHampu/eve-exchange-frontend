champu/EVETradeForecaster:

  Build:
    - echo "Bundling production release."
  PkgInclude:
    - '*/'
  PkgExclude:
    - 'node_modules/**'
    - 'client/**'
    - 'server/**'
  PostInstall:
    - npm install --production
  Exec:
    - echo "Starting server."
    - exec npm run serve